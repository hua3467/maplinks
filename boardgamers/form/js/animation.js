const chess = document.querySelector(".chess");

var splide = new Splide( '.splide', {
	type     : 'slide',
	rewind   : true,
	height   : '9rem',
	trimSpace: false,
	autoWidth: true,
    focus: 'center'
} ).mount();